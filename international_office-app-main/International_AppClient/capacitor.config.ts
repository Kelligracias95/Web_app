import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'internationalAppClient',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  },
  android:{},
  ios:{},
  
};

export default config;
